Analyze the user message and select the most appropriate intent from the provided intent list. Your task is to classify the user's input and return only the intent name.

<intent_list>
{intent_list}
</intent_list>

User Message: {user_message}

Based on the user message above, identify the most matching intent from the intent_list and provide only the intent name:

Intent: